procedure GNATCOLL.JSON.Support.Builder.Driver;
--  This is the top-level gnatstub driver. It reads the command-line arguments,
--  check the relations between gnatstub options and existing files and
--  creates a body sampler if possible and necessary.
