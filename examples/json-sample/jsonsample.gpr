with "gnatcoll.gpr";
with "aws.gpr";
with "../../gnatcoll-json.gpr";
project Jsonsample is

   for Source_Dirs use ("src");
   for Object_Dir use ".obj/" & project'Name;
   for Main use ("jsonsample-main.adb");
   package Compiler is
   -- for Default_Switches ("Ada") use ("-gnatd.n");
   end Compiler;
end Jsonsample;
